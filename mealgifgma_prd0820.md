# PRD — 밥집 찾기

> **버전** v0.2
> **작성일** 2026-08-19
> **상태** 핵심 방향 확정, API 실제 스펙만 남음 (§4.5, §10)
> **데이터 소스** 문화공공데이터광장/문화빅데이터플랫폼(한국문화정보원) 오픈API — "지역별 음식점 소비기반 트렌드"
> `openApiId=75493ae2-825d-49fb-893a-e2825d19fb9a` (id=603) / `API_CNV_063`
> **성격** 개인용 로컬 웹사이트. 서버·계정·배포 없음, 브라우저에서 여는 정적 페이지.

---

## 1. 개요

### 1.1 한 줄 정의
**시도 → 시군구 → 음식 카테고리**, 세 번의 선택만으로 해당 지역의 음식점 목록을 보여주는 웹 서비스.

### 1.2 배경
"어디서 뭘 먹지"를 정할 때 대부분의 서비스는 검색어 입력 · 로그인 · 위치 권한 · 광고 정렬을 요구한다.
이 서비스는 그 반대로 간다. **입력 없음 · 계정 없음 · 위치 권한 없음.** 드롭다운 세 개면 끝.

### 1.3 성공 기준
| 지표 | 목표 |
|---|---|
| 첫 진입 → 결과 목록 도달 | 3 인터랙션 이내 |
| 결과 목록 첫 렌더 | 1.5초 이내 (캐시 히트 시 300ms) |
| 오픈API 일일 호출량 | 개발 계정 한도(1,000건/일) 대비 30% 이하 |
| 데이터 결측 시 빈 화면 발생 | 0건 (항상 대체 UI) |

---

## 2. 대상 사용자 · 시나리오

**주 사용자:** 특정 지역을 정해두고 식당을 훑어보려는 사람
- 여행/출장 전 목적지 지역의 식당을 미리 둘러본다
- 약속 장소가 정해졌고 그 동네 식당 리스트가 필요하다

**비대상 (이번 범위 아님):** 실시간 예약, 리뷰 작성, 배달 주문

**핵심 시나리오**
1. 접속 → 시도 선택(`서울특별시`)
2. 시군구 선택(`마포구`)
3. 카테고리 선택(`한식`)
4. 목록 확인 → 카드 클릭 → 상세(이름·주소·전화)

---

## 3. 범위

### 3.1 In Scope (v1)
- 시도 / 시군구 / 카테고리 3단 필터
- 결과 목록 (페이지네이션 또는 무한 스크롤)
- 음식점 상세 뷰 (이름·카테고리·주소·전화, 텍스트 정보만)
- 결과 없음 · 오류 · 로딩 상태 처리
- 브라우저 내 응답 캐싱(메모리 or sessionStorage 대체 — 아티팩트 환경 아니므로 localStorage 사용 가능)

### 3.2 Out of Scope (v1)
- 로그인 / 즐겨찾기 / 리뷰 / 평점
- 지도 연동 (링크·임베딩·SDK 전부 제외 — 주소 텍스트로 충분)
- 현재 위치 기반 "내 주변" (v2 후보)
- 키워드 검색 (v2 후보)

---

## 4. 데이터 · API 연동 설계

> ⚠️ 이 장의 필드명·엔드포인트는 **KCISA 오픈API 공통 규약 기반 가정**이다.
> Swagger 원문 확인 후 §4.5 매핑 테이블만 갱신하면 나머지 설계는 그대로 유지된다.

### 4.1 요청 (가정)

```
GET http://api.kcisa.kr/openapi/service/rest/{service}/{operation}
  ?serviceKey={URL-encoded key}
  &numOfRows=100
  &pageNo=1
```

| 파라미터 | 필수 | 비고 |
|---|:---:|---|
| `serviceKey` | ✅ | UTF-8 URL 인코딩 필수 |
| `numOfRows` | ✅ | 페이지당 건수 |
| `pageNo` | ✅ | 1-base |
| 지역 필터 | 🔲 | **API가 시도/시군구 파라미터를 지원하는지 검증 필요** |
| 카테고리 필터 | 🔲 | **업종 파라미터 지원 여부 검증 필요** |

### 4.2 데이터 로딩 전략 — 직접 호출 우선, 안되면 폴백

개인용 로컬 페이지이므로 **1차로 브라우저 `fetch()`로 오픈API를 직접 호출**한다. 별도 서버·빌드 과정 없이 HTML/JS 파일 하나 열면 바로 동작하는 게 목표다.

**1차: 직접 호출**
```js
fetch(`https://api.kcisa.kr/openapi/service/rest/{service}/{operation}?serviceKey=${KEY}&numOfRows=100&pageNo=1&...`)
```
- `serviceKey`는 개인용 로컬 파일이라 클라이언트 코드에 그대로 둔다 (배포하지 않는다는 전제. **배포 전환 시 반드시 재검토**)
- API가 지역/업종 파라미터를 지원하면 그대로 위임, 지원 안 하면 전체를 받아 클라이언트에서 필터링

**막힐 경우 (CORS 차단 / 파라미터 미지원으로 페이지당 응답이 너무 큼) → 폴백**
- **폴백 A** — 최초 1회 전체 데이터를 수동으로 받아 로컬 JSON 파일로 저장해두고, 이후엔 그 파일만 읽는다 (재현 가능한 가장 단순한 방법)
- **폴백 B** — 로컬에서 아주 얇은 프록시(예: `python -m http.server`급 스크립트 하나)를 띄워 CORS만 우회

> 지금은 폴백을 미리 만들지 않는다. **직접 호출을 실제로 시도해보고, 막히는 지점을 확인한 다음 그 지점에 맞는 폴백을 붙인다.**

### 4.3 캐싱
- 같은 시도/시군구 조합은 재호출하지 않도록 메모리(또는 `localStorage`)에 응답을 캐싱
- 개인용 도구이므로 캐시 무효화 정책은 단순하게: "새로고침하면 그대로, 브라우저 데이터 지우면 초기화"

### 4.4 카테고리 정규화
원본 업종 데이터는 표기가 제각각이다 (`한식`, `경양식`, `기타`, `일식/횟집` 등).
서비스는 아래 **표시 카테고리**로 정규화한다.

| 표시 카테고리 | 매핑 대상(예시) |
|---|---|
| 한식 | 한식, 한정식, 백반, 국밥 |
| 중식 | 중식, 중국식 |
| 일식 | 일식, 초밥, 생선회, 횟집 |
| 양식 | 양식, 경양식, 이탈리안, 패밀리레스토랑 |
| 카페·디저트 | 카페, 커피숍, 제과, 전통찻집 |
| 기타 | 위 어디에도 속하지 않는 전부 |

- 매핑 테이블은 **단일 설정 파일**로 분리 (`categoryMap.ts`)
- **해당 지역에 0건인 카테고리는 선택지에서 아예 숨긴다** (빈 결과로 이어지는 선택을 원천 차단)

### 4.5 필드 매핑 테이블 🔲

| 서비스 내부 필드 | API 응답 필드 | 필수 | 결측 시 처리 |
|---|---|:---:|---|
| `id` | 🔲 | ✅ | 없으면 `이름+주소` 해시로 대체 생성 |
| `name` | 🔲 | ✅ | 없으면 해당 레코드 **제외** |
| `category` | 🔲 | ✅ | 없으면 `기타` |
| `sido` / `sigungu` | 🔲 | ✅ | 주소 문자열 파싱으로 역산 |
| `roadAddress` | 🔲 | — | 지번주소로 폴백 |
| `tel` | 🔲 | — | 필드 숨김 |
| `lat` / `lng` | 🔲 | — | 지도 버튼을 주소 검색 링크로 대체 |
| `description` | 🔲 | — | 필드 숨김 |

**원칙:** 이름 외 모든 필드는 없을 수 있다고 가정하고, 결측 시 **레이블째 숨긴다.** "정보 없음" 텍스트를 나열하지 않는다.

### 4.6 지역 목록 데이터
API가 시도/시군구 목록을 제공하지 않을 것을 전제로, **행정안전부 법정동코드 기반 정적 JSON 파일 하나**(HTML과 같은 폴더에 두는 로컬 파일)를 `<script>` 또는 `fetch`로 불러온다.
단, 실제 데이터가 존재하는 지역만 노출한다 (§4.4 카테고리와 동일 원칙).

---

## 5. 기능 요구사항

| ID | 기능 | 상세 | 우선순위 |
|---|---|---|:---:|
| F-01 | 시도 선택 | 17개 광역시도. 데이터 0건 지역은 비활성화 | P0 |
| F-02 | 시군구 선택 | 시도 선택 시 활성화. 미선택 시 disabled | P0 |
| F-03 | 카테고리 선택 | 해당 시군구에 실재하는 카테고리만 노출. 건수 배지 표시 | P0 |
| F-04 | 결과 목록 | 카드 리스트. 이름 / 카테고리 / 도로명주소 | P0 |
| F-05 | 상세 뷰 | 이름·카테고리·주소·전화 텍스트 표시 | P0 |
| F-06 | 상태 처리 | 로딩 / 0건 / API 오류(CORS 포함) | P0 |
| F-07 | URL 상태 동기화 | `?sido=11&sigungu=11440&cat=korean` — 해시/쿼리로 상태 유지, 새로고침 시 복원 | P1 |
| F-08 | 페이지네이션 | 20건 단위 "더 보기" | P1 |
| F-09 | 정렬 | 이름순 / 랜덤 셔플 | P2 |

### F-03 보충 — 선택 순서 제약
`시도 → 시군구 → 카테고리` 순서를 강제한다. 상위 선택이 바뀌면 하위 선택은 초기화한다.

### F-07 보충 — URL 설계
개인용 로컬 페이지라 "공유"보다는 **자기 자신이 새로고침해도 필터가 안 날아가게** 하는 목적이 크다. 쿼리스트링에 코드값을 담아 상태를 복원한다.

---

## 6. 화면 설계 · 디자인 톤

### 6.0 무드 — 사이버펑크
모래시계 프로젝트의 '콜드 블루 새벽'과는 완전히 다른 결. 여기는 **네온 사이니지가 걸린 뒷골목 노점 정보 단말기** 느낌을 목표로 한다.

| 요소 | 방향 |
|---|---|
| 배경 | 거의 검정에 가까운 딥 네이비/차콜 (`#0a0e17` 대) |
| 강조색 | 마젠타/시안 네온 2색 조합 (예: `#ff2ec4` + `#00f0ff`) — 카테고리별로 다른 네온 색 배정 가능 |
| 타이포 | 모노스페이스 또는 각진 고딕 계열. 카드 제목엔 살짝 글리치/스캔라인 느낌 |
| 인터랙션 | 호버 시 네온 테두리 발광, 선택 시 스캔라인 플리커 트랜지션 |
| 정보 밀도 | 터미널/HUD처럼 촘촘하게 — 라벨을 `>` `//` 같은 기호로 시작하는 것도 고려 |
| 절제 지점 | 가독성 최우선 항목(주소·전화번호)은 대비 낮은 배경에 흰색/밝은 회색으로, 네온은 장식·강조에만 사용 — 화려함이 정보 판별을 방해하면 안 됨 |

> 세부 컴포넌트(폰트 조합, 정확한 색상 팔레트, 글리치 애니메이션 스펙)는 프론트엔드 구현 단계에서 `frontend-design` 스킬을 참고해 확정한다.

### 6.1 메인 (선택 + 결과 동일 화면)

```
┌──────────────────────────────────────┐
│  ▓▓ 밥집 찾기 ▓▓                      │
│                                      │
│  [ 서울특별시 ▾ ] [ 마포구 ▾ ]        │
│                                      │
│  ⟦한식 42⟧⟦중식 8⟧⟦일식 15⟧⟦카페 63⟧ │
│    ▔▔▔▔                             │
│  ──────────────────────────────────  │
│  // 마포구 · 한식 · 42 RESULTS        │
│                                      │
│  ┌────────────────────────────────┐  │
│  │ ○○식당                한식     │  │
│  │ 서울 마포구 월드컵로 12         │  │
│  └────────────────────────────────┘  │
│  ┌────────────────────────────────┐  │
│  │ △△국밥                한식     │  │
│  └────────────────────────────────┘  │
│              [ 더 보기 ]              │
└──────────────────────────────────────┘
```

- 시도/시군구는 **셀렉트**, 카테고리는 **칩(chip)** — 카테고리는 이리저리 바꿔보는 대상이므로 한 탭에 전환되어야 한다
- 카테고리 칩에 **건수 배지**를 붙여 선택 전에 결과 규모를 알 수 있게 한다
- 필터 영역은 스크롤 시 상단 고정(sticky)

### 6.2 상세
바텀시트(모바일) / 모달(데스크톱). 페이지 전환 없이 목록 스크롤 위치를 보존한다.

```
> ○○식당
  한식 · 서울 마포구
  ─────────────────
  ADDR  서울 마포구 월드컵로 12
  TEL   02-000-0000
```

- 텍스트만 표시 (지도·전화걸기 버튼 없음 — F-05 참고). 전화번호는 눈으로 보고 직접 걸 수 있으면 충분.

### 6.3 상태별 화면
| 상태 | 처리 |
|---|---|
| 초기 (시도 미선택) | "지역을 선택해 주세요" + 시도 선택 유도 |
| 로딩 | 스켈레톤 카드 또는 터미널풍 로딩 인디케이터 |
| 0건 | "이 조건에 등록된 음식점이 없어요" + 카테고리 초기화 버튼 |
| API 오류 (CORS 포함) | "정보를 불러오지 못했어요" + 원인 힌트(CORS 차단 가능성) + 재시도 버튼 |

---

## 7. 비기능 요구사항

| 항목 | 기준 |
|---|---|
| 배포 | 없음. HTML 파일을 더블클릭해 열거나 `file://`/간단한 로컬 서버로 실행 |
| 성능 | 결과 렌더는 API 응답 속도에 종속 (개인용이므로 엄격한 기준 불필요) |
| 반응형 | 데스크톱 우선. 모바일은 참고용 (개인 로컬 사용 특성상 우선순위 낮음) |
| 브라우저 | 사용자의 평소 브라우저 1종 기준으로 동작하면 충분 |
| 데이터 갱신 | 원본이 연 단위 갱신이므로 수동으로 필요할 때만 새로고침 |
| 출처 표기 | 화면 하단에 데이터 출처(한국문화정보원) 명시 |

---

## 8. 예외 · 엣지 케이스

1. **CORS로 직접 호출이 막힘** → §4.2 폴백(로컬 JSON 저장 / 얇은 프록시)으로 전환. 화면엔 이 상황을 있는 그대로 안내
2. **일일 호출 한도(1,000건) 초과** → 개인용 저빈도 사용이라 가능성 낮음. 초과 시 에러 메시지에 "내일 다시 시도" 안내
3. **주소 문자열로 시군구 역산 실패** → `기타 지역`으로 분류하지 않고 해당 레코드 제외 (잘못된 지역에 노출되는 것보다 안 보이는 게 낫다)
4. **동일 음식점 중복 레코드** → `이름 + 도로명주소` 기준 dedupe
5. **폐업/이전 데이터** → 원본이 연 단위 갱신이므로 **"정보 기준일"을 화면에 명시**하고 최신성을 보증하지 않음을 밝힌다

---

## 9. 마일스톤

| 단계 | 내용 | 산출물 |
|---|---|---|
| **M0** | API 실제 스펙 확인 · 브라우저 직접 호출 테스트 (CORS 여부 포함) | §4.5 매핑 테이블 확정, 폴백 필요 여부 판단 |
| **M1** | 지역/카테고리 정적 데이터 준비 (§4.4, §4.6) | `regions.json`, `categoryMap.js` |
| **M2** | 3단 필터 + 결과 목록, 사이버펑크 스타일 적용 (F-01~F-04, F-06, F-07) | 동작하는 `index.html` |
| **M3** | 상세 뷰 (F-05) | v1 완성 |
| **M4** | 페이지네이션 · 정렬 (F-08, F-09) | v1.1 |

---

## 10. 확정 사항 요약

| 항목 | 결정 |
|---|---|
| 지역 단위 | 시도 → 시군구 (2단계) |
| 플랫폼 | 웹사이트, 개인용 로컬 배포 (서버·계정 없음) |
| 기술 스택 | 순수 HTML/JS, 빌드 도구 없음 |
| 데이터 로딩 | 브라우저 직접 호출 우선, 막히면 그때 폴백 설계 |
| 카테고리 | 6개 대분류 (한식/중식/일식/양식/카페·디저트/기타) |
| 지도 연동 | 없음. 주소는 텍스트로만 표시 |
| 서비스명 | 밥집 찾기 |
| 디자인 톤 | 사이버펑크 (네온 마젠타/시안, 다크 배경, 터미널/HUD 느낌) |

## 11. 남은 미확정 사항 🔲

1. **API 실제 스펙** — 필드명, 지역/업종 파라미터 지원 여부 (컬럼정의서 확보 시 §4.5 즉시 갱신)
2. **CORS 여부** — 직접 호출을 실제로 시도해봐야 확정. 막히면 §4.2 폴백 중 어느 쪽으로 갈지 결정 필요
